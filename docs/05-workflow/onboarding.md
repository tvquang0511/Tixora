## Onboarding ngắn cho thành viên mới

1. Fork hoặc clone repo
2. Cài dependencies và setup hook local (chạy 1 lần):
	- `npm install`
	- `npm run setup:hooks`
3. Đọc `workflows.md` và `CONTRIBUTING.md`
4. Khi tạo branch: theo `feat/`, `fix/` quy ước
5. Tạo PR sử dụng template, link issue nếu có
